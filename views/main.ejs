<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Get Vehicle Brochure | 95 Muscle</title>
    <style>
      :root {
        --primary-color: #4caf50;
        --primary-dark: #45a049;
        --background-color: #f0f8f0;
        --text-color: #333;
      }
      body {
        font-family: "Arial", sans-serif;
        background-color: var(--background-color);
        background-image: url("/imgs/background.jpg"); /* Set the background image */
        background-size: cover; /* Adjust the size to cover the entire background */
        background-repeat: no-repeat; /* Ensure the background doesn't repeat */
        background-position: center; /* Position the image in the center */
        color: var(--text-color);
        line-height: 1.6;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        position: relative; /* To position the green overlay */
      }

      body::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(92, 255, 92, 0.5); /* Semi-transparent green overlay */
        mix-blend-mode: multiply; /* Blend the overlay with the background image */
        z-index: -1; /* Keep the overlay behind other content */
      }
      .container {
        background-color: black;
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 400px;
        width: 100%;
      }
      .logo {
        max-width: 200px;
        margin-bottom: 1.5rem;
      }
      form {
        display: flex;
        flex-direction: column;
      }
      label {
        margin-bottom: 0.5rem;
        font-weight: bold;
        color: var(--primary-color);
      }
      input[type="text"] {
        padding: 0.75rem;
        margin-bottom: 1.5rem;
        border: 2px solid var(--primary-color);
        border-radius: 6px;
        font-size: 1rem;
        color: white;
        background-color: black;
      }
      input[type="text"]:focus {
        outline: none;
        box-shadow: 0 0 0 2px rgba(76, 175, 80, 0.3);
        background-color: black;
      }
      button {
        background-color: var(--primary-color);
        color: white;
        border: none;
        padding: 1rem;
        font-size: 1.1rem;
        border-radius: 6px;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.1s ease;
        text-transform: uppercase;
        font-weight: bold;
        letter-spacing: 0.5px;
      }
      button:hover {
        background-color: var(--primary-dark);
      }
      button:active {
        transform: translateY(1px);
      }

    </style>
  </head>
  <body>
    <div class="container">
      <img src="/imgs/logo.png" alt="95 Muscle Logo" class="logo" />
      <form action="/scrape" method="POST" id="scrapeForm">
        <label for="url">Vehicle URL:</label>
        <input
          type="text"
          id="url"
          name="url"
          required
          placeholder="Enter vehicle URL here"
        /> 
        <button type="submit">Get Brochure</button>
      </form>
    </div>
  </body>
</html>
